// Compatibility barrel for the mailbox HTTP surface.
// Route implementations remain in `_internal/router` until the route layer is split further.
export { mailboxRouter } from "./_internal/router";
