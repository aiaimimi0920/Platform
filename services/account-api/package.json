{
  "name": "@neuro/account-api",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "prebuild": "npm run build --workspace @neuro/account-domain",
    "pretypecheck": "npm run build --workspace @neuro/account-domain",
    "build": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "pretest": "npm run build --workspace @neuro/backend-foundation",
    "test": "node --test --import tsx \"src/**/*.test.ts\"",
    "test:integration:required": "node -e \"console.log('No required integration suites for @neuro/account-api')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/multipart": "^9.2.1",
    "@neuro/account-domain": "*",
    "@neuro/backend-foundation": "*",
    "@neuro/contracts": "*",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3"
  }
}
