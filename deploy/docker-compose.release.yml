name: neuro-platform

# Artifact-only deployment contract. The release builder replaces every image
# token with an immutable Platform image reference. Database, Valkey, object
# storage, Gateway, Tea, and identity endpoints are operator-supplied through
# PLATFORM_RUNTIME_ENV_FILE; no source directory or sibling repository is used.
services:
  core-migrate:
    image: "@@CORE_IMAGE@@"
    restart: "no"
    healthcheck:
      disable: true
    env_file:
      - "${PLATFORM_RUNTIME_ENV_FILE:?Set PLATFORM_RUNTIME_ENV_FILE to a populated runtime env file}"
    command: ["npm", "run", "db:migrate", "--workspace", "@neuro/core"]

  gateway-domain-migrate:
    image: "@@ACCOUNT_API_IMAGE@@"
    restart: "no"
    healthcheck:
      disable: true
    env_file:
      - "${PLATFORM_RUNTIME_ENV_FILE:?Set PLATFORM_RUNTIME_ENV_FILE to a populated runtime env file}"
    command: ["npm", "run", "db:migrate", "--workspace", "@neuro/ai-gateway-domain"]
    depends_on:
      core-migrate:
        condition: service_completed_successfully

  account-domain-migrate:
    image: "@@ACCOUNT_API_IMAGE@@"
    restart: "no"
    healthcheck:
      disable: true
    env_file:
      - "${PLATFORM_RUNTIME_ENV_FILE:?Set PLATFORM_RUNTIME_ENV_FILE to a populated runtime env file}"
    command: ["npm", "run", "db:migrate", "--workspace", "@neuro/account-domain"]
    depends_on:
      gateway-domain-migrate:
        condition: service_completed_successfully

  core:
    image: "@@CORE_IMAGE@@"
    init: true
    restart: unless-stopped
    env_file:
      - "${PLATFORM_RUNTIME_ENV_FILE:?Set PLATFORM_RUNTIME_ENV_FILE to a populated runtime env file}"
    environment:
      PORT: "4000"
    ports:
      - "127.0.0.1:${CORE_HOST_PORT:-4028}:4000"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:4000/ready').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    depends_on:
      core-migrate:
        condition: service_completed_successfully

  account-api:
    image: "@@ACCOUNT_API_IMAGE@@"
    init: true
    restart: unless-stopped
    env_file:
      - "${PLATFORM_RUNTIME_ENV_FILE:?Set PLATFORM_RUNTIME_ENV_FILE to a populated runtime env file}"
    environment:
      PORT: "4000"
    ports:
      - "127.0.0.1:${ACCOUNT_API_HOST_PORT:-4128}:4000"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:4000/ready').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    depends_on:
      account-domain-migrate:
        condition: service_completed_successfully
      core:
        condition: service_healthy

  account-worker:
    image: "@@ACCOUNT_WORKER_IMAGE@@"
    init: true
    restart: unless-stopped
    env_file:
      - "${PLATFORM_RUNTIME_ENV_FILE:?Set PLATFORM_RUNTIME_ENV_FILE to a populated runtime env file}"
    environment:
      WORKER_HEALTH_PORT: "7303"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:7303/ready').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    depends_on:
      account-domain-migrate:
        condition: service_completed_successfully
      account-api:
        condition: service_healthy

  worker:
    image: "@@WORKER_IMAGE@@"
    init: true
    restart: unless-stopped
    env_file:
      - "${PLATFORM_RUNTIME_ENV_FILE:?Set PLATFORM_RUNTIME_ENV_FILE to a populated runtime env file}"
    environment:
      WORKER_HEALTH_PORT: "7301"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:7301/ready').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    depends_on:
      core-migrate:
        condition: service_completed_successfully
      core:
        condition: service_healthy

  executor:
    image: "@@EXECUTOR_IMAGE@@"
    init: true
    restart: unless-stopped
    env_file:
      - "${PLATFORM_RUNTIME_ENV_FILE:?Set PLATFORM_RUNTIME_ENV_FILE to a populated runtime env file}"
    environment:
      EXECUTOR_HEALTH_PORT: "7302"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:7302/ready').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    depends_on:
      core-migrate:
        condition: service_completed_successfully
      core:
        condition: service_healthy

  web:
    image: "@@WEB_IMAGE@@"
    init: true
    restart: unless-stopped
    env_file:
      - "${PLATFORM_RUNTIME_ENV_FILE:?Set PLATFORM_RUNTIME_ENV_FILE to a populated runtime env file}"
    environment:
      NODE_ENV: production
      PORT: "3000"
    ports:
      - "127.0.0.1:${WEB_HOST_PORT:-3028}:3000"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/ready').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    depends_on:
      core:
        condition: service_healthy
      account-api:
        condition: service_healthy
